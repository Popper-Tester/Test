badge-server-url: http://badges.falsifiable.us
metadata:
  access_right: open
  license: CC-BY-4.0
  publication_type: article
  upload_type: publication
pipelines:
  co2-emissions:
    envs:
      host:
        args: []
    parameters: []
    path: pipelines/co2-emissions
    requirements: {}
    stages:
    - get_data
    - run
    - validate
  my_pipeline:
    envs:
      host:
        args: []
    parameters: []
    path: pipelines/my_pipeline
    requirements: {}
    stages:
    - one
    - two
    - three
search_sources:
- popperized
version: 1
